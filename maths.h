#ifndef MATHS_H
#define MATHS_H

/** Contient des macros mathématiques utile
 */

//macro pour le calcul de valeur absolue
#define abs(x) ((x) < 0 ? - (x) : (x))
//macro pour le signe
#define sgn(x) ((x) < 0 ? - (1) : (1))


#endif /* MATHS_H */
